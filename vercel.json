{
  "builds": [
    {
      "src": "server/index.mjs",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/index.mjs"
    },
    {
      "src": "/(.*)",
      "dest": "/dist/$1"
    }
  ]
}
